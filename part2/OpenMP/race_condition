int i, A[MAX]; 
#pragma omp parallel for 
for (i=0;i< MAX; i++) { 
  int j = 5 + 2*(i+1); 
  A[i] = big(j); 
 }
 
 //failed to come up with the code below 
 
 //#include <omp.h>
//#define MAX 100

//int main() {
  //  int i, A[MAX];

    //#pragma omp parallel for
    //for (i = 0; i < MAX; i++) {
        //int j = 5 + 2 * (i + 1);

        //#pragma omp critical
        //{
          //  A[i] = big(j);
        //}
    //}

    //return 0;
//}
